import React from 'react';
import {storiesOf} from '@storybook/react';
import WaitSpinner from './WaitSpinner';

storiesOf('WaitSpinner', module).add('Full Screen', () => <WaitSpinner></WaitSpinner>);
